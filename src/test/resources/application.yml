feign:
  hystrix:
    enabled: false
apps:
  discord:
    hookHost: "https://discordapp.com"
    hookUri: "/api/webhooks/HOOK/PATH"
tailer:
  enabled: false
  logFile: "server.log"
  readDelayMillis: 1000
  readFromEnd: true
  closeFileBetweenChunks: true